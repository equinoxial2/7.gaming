# 7.Gaming Demo

This project is a small showcase website. A lightweight SQLite database is now used to store users.

## Initialisation de la base

```bash
php init_db.php
```

This will create `data/database.sqlite` and add a demo account (`demo/demo`).

